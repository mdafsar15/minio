server.port=8888

# Minio Host
spring.minio.url=http://192.168.0.179:80
#spring.minio.url=https://obj.lcb1.integrandtech.com
# Minio Bucket name for your application
spring.minio.bucket=lcb
# Minio access key (login)
spring.minio.access-key=XWCPqmGiXBcystBy
# Minio secret key (password)
spring.minio.secret-key=jDeKxGn8v38yF5fX2JXS8ZdDompKBkCE

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=500MB
# Max Request Size
spring.servlet.multipart.max-request-size=100MB

management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
